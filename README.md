# java-lotto-precourse

## 프로젝트 구조

```
src/main/java/lotto/
├── Application.java                 # 애플리케이션 진입점
├── domain/                          # 도메인 계층
│   ├── Lotto.java                  # 로또 번호 (불변 객체)
│   ├── WinningLotto.java           # 당첨 로또 (당첨 번호 + 보너스 번호)
│   ├── Lottos.java                 # 로또 목록 (일급 컬렉션)
│   ├── LottoMachine.java           # 로또 발행기 (정적 유틸리티)
│   ├── LottoGame.java              # 로또 게임 로직 관리
│   ├── LottoResults.java           # 당첨 결과 집계
│   ├── Prize.java                  # 당첨 등수 Enum
│   ├── RandomLottoNumberGenerator.java  # 로또 번호 생성기
│   ├── constants/                  # 상수 관리
│   │   ├── LottoConstant.java
│   │   └── ExceptionMessage.java
│   ├── dto/                        # DTO 계층
│   │   ├── LottoBuyResultDto.java
│   │   └── LottoResultDto.java
│   └── validator/                  # 검증 로직 분리
│       ├── LottoValidator.java
│       └── LottoMachineValidator.java
├── presentation/                    # 프레젠테이션 계층
│   ├── LottoGameApp.java           # 게임 플로우 컨트롤러
│   ├── InputView.java              # 입력 담당
│   └── OutputView.java             # 출력 담당
└── util/                            # 유틸리티 계층
    ├── CsvParser.java              # CSV 파싱
    └── IntegerParser.java          # 문자열 → 정수 파싱
```

---

## 클래스 다이어그램

![Domain Class Diagram.png](images%2FDomain%20Class%20Diagram.png)

---

## 고민했던 부분

### 1. **당첨 로또 생성 과정 재입력 로직**

#### 배경

- 당첨 번호와 보너스 번호는 **서로 독립된 입력 단계**로 동작합니다.
- 각 단계에서 잘못된 입력이 들어오면 **해당 단계부터 다시 입력**받아야 했습니다.

**검토했던 방법**

| 방법               | 장점                 | 단점               |
|------------------|--------------------|------------------|
| **Setter 사용**    | 각 단계별 검증 가능, 구현 단순 | 불변성 훼손           |
| **Builder 패턴**   | 단계별 생성 분리          | 매개변수 관리 목적이라 부적합 |
| **Validator 추가** | 단계별 검증 가능, 불변성 유지  | 검증 로직 중복 발생      |

#### 최종 결정

- **Validator를 추가**하여 각 단계 입력 이후 LottoGameApp에서 검증하여 예외 처리 가능하도록 구성했습니다.
- `WinningLotto`를 불변 객체로 관리할 수 있었음
- 입력과 도메인 객체에서 중복된 검증이 동작하지만, 어쩔 수 없다고 판단했습니다.

---

### 2. **일부 클래스를 Static 메서드로 관리**

#### LottoMachine, RandomNumberGenerator

- 로또 머신은 **상태가 필요 없다고 판단했습니다.**
- 로또 게임 규칙은 **규칙 변경 가능성이 매우 낮다고 판단했습니다.**

---

### 3. **등수 판별 로직을 Prize Enum에 위임**

#### 고민

- 등수를 산정하는 로직을 어디에 두어야 하나?
    - `Lotto`에서 등수를 정하자니, `Lotto`의 역할이 아닌 것 같았습니다.
    - 별도 객체를 만들자니 그 역할 하나만을 위해 클래스를 만드는 것이 과한 것 같았습니다.

#### 해결책

- 등수를 표현하는 `Prize` Enum에서 함수형 인터페이스(`BiPredicate`)를 이용해 자동 판단하도록 구성했습니다.
- 복권 당첨 정보(일치 개수, 보너스 여부)를 넘기면 적절한 `Prize`를 반환합니다.

---

### 4. **정적 팩토리 메서드를 사용하지 않음**

이유:

- 생성자가 하나뿐인 단순한 도메인 객체였음
- 데이터 캐싱이나 복잡한 생성 로직이 필요 없음

---

### 5. **Getter 사용 (불변 컬렉션 반환)**

#### 배경

- `Lotto`는 `List<Integer>` (불변 객체)를 가진 컬렉션
- List만 불변 컬렉션(`List.copyOf()`)로 반환하면 외부 수정 불가

#### 판단 근거

- `Integer`라는 내부 객체가 다른 자료형으로 바뀔 가능성이 매우 낮았습니다.
- 외부에 노출되어도 상관없다고 판단했습니다.
- **방어적 복사만 생성 시 수행**하고 Getter로 불변 리스트 반환하도록 했습니다.

---

### 6. **로또 번호를 별도 클래스로 추상화하지 않음**

- `Integer`를 `LottoNumber` 같은 별도 클래스로 만들까?

#### 판단

- 검증할 내용은 **범위(1~45)** 밖에 없었습니다.
- 나머지 기능은 이미 `Integer`에서 제공하는 기능이었습니다.
- 새로운 클래스를 만드는 것이 오히려 혼란을 가중시킬 것이라고 판단했습니다.

---

### 7. **재입력 로직의 원자성 고려**

#### 고려사항

1. 재입력 로직 처리 과정에서 예외가 발생하기 전까지의 단계 중 **롤백되어야 하는 부분**이 있는지 판단했습니다.
2. 해당 애플리케이션에는 그럴 포인트가 없어 보여 롤백은 신경 쓰지 않았습니다!
3. **`while`문으로 정상 입력이 들어올 때까지 간단하게 처리했습니다!**

---

## 기능 목록

### 입력

#### 로또 구입 금액

- [x] 양수를 입력받는다
- [x] [예외] 숫자가 아닌 값이 입력되면 IllegalArgumentException 발생

#### 당첨 번호

- [x] 번호는 쉼표(,)를 기준으로 구분한다
- [x] 입력 사이 공백 허용 (예: 1, 2, 3)
- [x] 숫자 사이의 공백은 허용하지 않음 (예: "1 2")
- [x] [예외] 쉼표(,)와 숫자가 아닌 입력이 들어오면 IllegalArgumentException 발생
- [x] [예외] 숫자 사이에 공백이 입력되면 IllegalArgumentException 발생

#### 보너스 번호

- [x] 숫자를 입력받는다
- [x] [예외] 숫자가 아닌 값이 입력되면 IllegalArgumentException 발생

---

### 출력

- [x] 발행한 로또 수량 출력
- [x] 발행한 로또 번호 출력 (오름차순 정렬)
- [x] 당첨 내역 출력
- [x] 수익률 출력
    - [x] 소수 둘째 자리에서 반올림
- [x] 예외 메시지 출력

---

### 로또 발행

- [x] 로또를 발행한다

---

### 로또 추첨

- [x] 당첨 번호를 입력으로 받는다
- [x] [예외] 로또 번호와 보너스 번호가 중복되면 IllegalArgumentException 발생

---

### 당첨 여부 확인

다음 기준에 따른 당첨 결과를 판단한다:

- [x] **1등**: 6개 번호 일치 → 2,000,000,000원
- [x] **2등**: 5개 번호 + 보너스 번호 일치 → 30,000,000원
- [x] **3등**: 5개 번호 일치 → 1,500,000원
- [x] **4등**: 4개 번호 일치 → 50,000원
- [x] **5등**: 3개 번호 일치 → 5,000원

---

### 수익률 계산

- [x] 수익률 = (총 당첨 금액 / 총 구매 금액) × 100
- [x] 소수 둘째 자리에서 반올림

---

### 도메인 설계

#### 로또 (Lotto)

- [x] 로또 번호의 숫자 범위는 1 ~ 45
- [x] 중복되지 않는 숫자 6개
- [x] 로또는 오름차순으로 정렬된다
- [x] [예외] 범위를 벗어나는 경우 IllegalArgumentException 발생
- [x] [예외] 6자리가 아니면 IllegalArgumentException 발생
- [x] [예외] 번호가 중복되면 IllegalArgumentException 발생

---

#### 당첨 로또 (WinningLotto)

- [x] 중복되지 않는 숫자 6개 + 보너스 번호 1개
- [x] 로또 번호와 보너스 번호 모두 중복되지 않아야 함
- [x] [예외] 보너스 번호가 로또 번호 중복되면 IllegalArgumentException 발생

---

#### 로또 발행기 (LottoMachine)

- [x] 금액을 입력받아 로또를 발행
- [x] 로또 1장당 가격 단위(1000원) 검증
- [x] 최대 구매 가능한 로또 개수는 1,000,000개
- [x] [예외] 1장 미만 구매 시 IllegalArgumentException 발생
- [x] [예외] 1,000,000장 초과 구매 시 IllegalArgumentException 발생

---

#### 로또 목록 (Lottos)

- [x] 여러 장의 로또를 관리
- [x] 당첨 로또와 비교하여 당첨 결과 판단
- [x] 당첨 등수별 결과 반환 (Enum 활용)

---

#### 로또 영수증 (LottoResults)

- [x] 구매한 로또 정보 보관
- [x] 당첨 내역 정보 제공 (필요 시)
- [x] 수익금 계산

---

#### 로또 실행기 (LottoGameApp)

- [x] 입력 및 로또 게임 흐름 제어

---

### 예외 처리 규칙

- [x] 잘못된 값 입력 시 IllegalArgumentException 또는 IllegalStateException 발생
- [x] 에러 메시지는 [ERROR]로 시작
- [x] 예외 발생 시 해당 부분부터 입력을 다시 받음
- [x] Exception이 아닌 명확한 예외 유형 사용

---
